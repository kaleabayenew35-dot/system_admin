# TG Games Admin

Vite + React admin interface for managing games.

Quick start:

1. Install dependencies

```bash
cd admin
npm install
```

2. Run development server

```bash
npm run dev
```

The admin UI expects the backend at `http://localhost:5000` by default. Configure `VITE_BACKEND_URL` in `.env` if different.
